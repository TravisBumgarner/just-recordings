{
  "name": "@just-recordings/desktop",
  "version": "0.0.1",
  "description": "Just Recordings Desktop App",
  "type": "module",
  "main": "./out/main/index.js",
  "author": {
    "name": "Travis Bumgarner",
    "email": "11890057+TravisBumgarner@users.noreply.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/TravisBumgarner/just-recordings.git"
  },
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "postbuild": "node scripts/copy-web-dist.js",
    "preview": "electron-vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:win": "npm run build && electron-builder --win --config",
    "build:mac": "npm run build && electron-builder --mac --config",
    "build:linux": "npm run build && electron-builder --linux --config",
    "publish": "npm run build && electron-builder --publish always"
  },
  "dependencies": {
    "@electron-toolkit/preload": "^3.0.1",
    "@electron-toolkit/utils": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "electron": "28.1.0",
    "electron-builder": "^25.1.8",
    "electron-vite": "^2.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.11",
    "vitest": "^1.2.2"
  }
}
