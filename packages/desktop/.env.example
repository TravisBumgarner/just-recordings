# API Configuration
# Full URL to the backend API (no proxy in Electron)
VITE_API_URL=http://localhost:3001/api

# Sentry Configuration (optional - only used in production/packaged builds)
# SENTRY_DSN=https://your-key@sentry.io/your-project
