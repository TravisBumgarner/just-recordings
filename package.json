{
  "name": "just-recordings",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "concurrently -n api,web -c blue,green \"npm run dev -w @just-recordings/api\" \"npm run dev -w @just-recordings/web\"",
    "dev:desktop": "concurrently -n api,desktop -c blue,yellow \"npm run dev -w @just-recordings/api\" \"npm run dev -w @just-recordings/desktop\"",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down",
    "db:push": "npm run db:push -w @just-recordings/api"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
